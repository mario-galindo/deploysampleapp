//
//  ContentView.swift
//  deploysampleapp
//
//  Created by Mario Galindo on 22/1/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world for deploy!")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
